את עובדת בתוך פרויקט Claude Code.

מסמך הנדסי פורמלי מצורף לסשן הזה:
**`claude_md_instructions_recommendation.docx`**

הוא מגדיר את הפיצול הארכיטקטוני הנכון בין:
- CLAUDE.md (כללים ברמת המערכת)
- INSTRUCTIONS (ביצוע ברמת המשימה)

העבודה שלך היא לא להמציא מבנה חדש.
העבודה שלך היא ליישם בקפדנות את ההמלצות ההנדסיות מהמסמך הזה
על הפרויקט הנוכחי.

---

## שלב 1 — טעני את הסטנדרט ההנדסי

קראי את המסמך המצורף: `claude_md_instructions_recommendation.docx`

התייחסי אליו כ:
- סמכותי
- מחייב
- מפרט עיצוב לא-ניתן-למשא-ומתן

אל תסכמי באופן רופף.
חלצי את הכללים שלו במפורש.
שימי לב שהמסמך מכיל תבנית CLAUDE.md מוכנה — השתמשי בה כ-scaffold.

אם המסמך לא מצורף או לא ניתן לקריאה, עצרי ובקשי ממני לצרף אותו.
אל תמשיכי מזיכרון או מידע כללי.

---

## שלב 2 — בני את המבנה מתוך המסמך

שימוש רק בכללים מהמסמך המצורף:
1. הגדירי מה שייך ל-CLAUDE.md
2. הגדירי מה שייך ל-INSTRUCTIONS
3. הגדירי את הגבול ביניהם
4. הגדירי כללי פתרון קונפליקטים
5. הגדירי מה שייך מחוץ לשניהם (hooks / permissions / CI)

אל תוסיפי רעיונות שלא נמצאים במסמך.
אל תפשטי מעבר למה שכתוב.

---

## שלב 3 — יישמי על הפרויקט הנוכחי

לפני מיפוי של משהו, חקרי את הרפו:
- רשמי קבצים ותיקיות ברמה העליונה
- קראי את README וכל CLAUDE.md קיים
- זהי את ה-stack (package.json / pyproject.toml / Cargo.toml / וכו')
- זהי פקודות build / test / lint אמיתיות
- זהי מסלולים מוגנים (.env, migrations/, secrets/, וכו')
- אם CLAUDE.md קיים נוכח, אל תדרסי בשקט —
  הציעי diff במקום
- אם מידע קריטי חסר ולא ניתן להסיק מהרפו,
  עצרי ושאלי אותי. אל תנחשי. אל תשתמשי ב-placeholders.

לאחר מכן, בשימוש בתבנית CLAUDE.md מהמסמך המצורף כ-scaffold:
- מפי כל אלמנט אמיתי של הפרויקט הזה אל:
  → CLAUDE.md
  → INSTRUCTIONS
  → .claude/rules/
  → hooks / permissions / CI
- עבור כל סעיף בתבנית:
  → אם הוא חל על הפרויקט הזה, מלאי אותו בתוכן ספציפי לפרויקט
  → אם הוא חל חלקית, התאימי וקצצי
  → אם הוא לא חל, מחקי את הסעיף לחלוטין
- אם משהו לא שייך בבירור לשום מקום,
  סמני אותו במפורש כ-"UNDEFINED BY STANDARD"

---

## שלב 4 — אכפי משמעת מבנית

ודאי:
- אין כפילות בין CLAUDE.md ל-INSTRUCTIONS
- אין דליפה של כללי מערכת להנחיות משימה
- אין לוגיקת משימה בתוך CLAUDE.md
- אין טקסט תבנית גנרי שלא באמת נכון לפרויקט הזה

---

## שלב 5 — Deliverable

1. הראי לי דוח גילוי קצר:
   - Stack מזוהה
   - פקודות Build / test / lint שנמצאו
   - מסלולים מוגנים שזוהו
   - סטטוס CLAUDE.md קיים
   - שאלות פתוחות שמשפיעות על תוכן

2. אם יש שאלות פתוחות שמשפיעות על תוכן, שאלי אותי לפני הכתיבה.
   אל תמשיכי עם הנחות.

3. ברגע שנפתר, כתבי את הקובץ ישירות ל-./CLAUDE.md.
   אל רק תדפיסי אותו לצ'אט.

4. בנפרד, רשמי בצ'אט:
   - פריטים מנותבים ל-tasks/<TASK-ID>/INSTRUCTIONS.md
   - פריטים מנותבים ל-.claude/rules/
   - פריטים שדורשים אכיפת hooks / permissions / CI
   - כל דבר שסומן "UNDEFINED BY STANDARD"

---

## כללים נוקשים

- המסמך המצורף `claude_md_instructions_recommendation.docx` הוא
  מקור הכללים הארכיטקטוני היחיד. אל תחליפי אותו בידע קודם.
- אל תמציאי כללים ארכיטקטוניים מעבר למסגרת של המסמך
- תארי את ה-stack, הארכיטקטורה, והפקודות האמיתיים של הפרויקט הזה
  (המסגרת של המסמך היא ה-scaffold; המציאות של הפרויקט ממלאת אותה)
- אל תסתמכי על הנחות קודמות
- אל תערבבי שכבות
- אל תעשי אופטימיזציה מעבר למסמך
- מחקי באגרסיביות סעיפי תבנית שלא חלים על הפרויקט הזה
  (CLAUDE.md נטען בכל סשן — קצר יותר עדיף)
- אם המסמך לא ברור → אמרי זאת במפורש
- אם הרפו לא ברור → שאלי, אל תנחשי
