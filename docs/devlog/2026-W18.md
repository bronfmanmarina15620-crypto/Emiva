# שבוע של 2026-04-27 (W18)

*כל ה-commits בשבוע זה נפלו ביום ראשון 27.4 — יום עבודה צפוף שסגר את v1 של אוולין.*

## עלה

- **MATH-EVELYN-MONEY-001** — ערבוב 3-מ-5 בעיות-כסף בתוך `add_sub_100` ו-`multiplication`. שדה `context?: "money"` אופציונלי על `AddSubItem` / `MultItem`; 60 פריטים חדשים בעברית (6 לכל דרגה × 2 מאגרים); לוגיקת `nextDesiredContext` שמאפסת כל 5 פריטים; רינדור משפט עברי ₪ במקום סימון מתמטי. נאמן ל-MyLevel §3.1 + §5.4. 306→322 בדיקות.
- **CORE-HEBREW-EVELYN-002** — מיומנות `hebrew_comprehension` לאוולין (סוף שרשרת גיל 7–8). 30 פריטים — פסקה קצרה + 2 שאלות אמריקאיות, 4 אפשרויות + הסבר embedded, 5 דרגות (עובדתי → הסקה רב-שלבית). `currentQuestionIndex` state בדף הסשן; Q2 על אותו פריט לפני קידום. 322→336 בדיקות.
- **Items per session מגיל פרופיל** — `itemsPerSessionForAge()` חדש: גיל 7–8 → 15, גיל 9–10 → 18. הוציא הגדרה מ-`.env.local` (drift risk).
- **Parent gate** — כפתור צד-הורה בדף הבית עם gate-code אופציונלי.
- **Shared `.claude/` permissions** + `.gitignore` local override — מונע friction בין-מכשירי.

## הפתעות

- **BL-003 נפתח ונסגר באותו יום.** המפרט הראשוני תיאר `money_words` כמיומנות עצמאית אחרי כפל. קריאה צמודה של MyLevel §3.1 + §5.4 הראתה שהערבוב הוא הוראת הליבה — לא variant "אם משהו". המפרט נכתב מחדש ב-commit העוקב; BL-003 עבר ישר לסגורים.
- **CORE-HEBREW-EVELYN-001 (פוניקה) בוטל.** שתי הבנות קוראות בשטף; פוניקה שיטתית הייתה מתחת לרמה ולא מוסיפה ערך. דילוג מודע, לא נפילה — תועד ב-memory ובפרופיל.

## תקוע

- **אין משימה פעילה ב-Now.** כל v1 של אוולין הושלם — מתמטיקה + הבנת הנקרא. ה-ROADMAP ב-"אין פעיל" עד שמישהי מושכת את CORE-HEBREW-EMILIA-001 פנימה.
- **QA ידני** — עדיין לא ישבנו עם הבנות על אף מסלול. בדיקות עוברות; מציאות לא נבדקה.
- **BL-002 (שבריריות webpack cache)** — טריגר פעיל אחד מאז 2026-04-23 (שבירה בלי שינוי מבני). עדיין לא הגיע לסף לקיחה (≥ 5 שבירות בשבוע · שבירה בסשן של בת).

## הבא

- **CORE-HEBREW-EMILIA-001** — הבנת הנקרא לאמיליה (טקסטים מאתגרים + הסקה מתקדמת + אוצר מילים). הבא בתור לפי ROADMAP.
- **QA ידני עם הבנות** — לפני כל מסלול חדש. זה החוב הטכני הגדול ביותר כרגע.
- **מסלול אנגלית** — אחרי CORE-HEBREW-EMILIA-001 (לפי CLAUDE.md §המוצר).

## Snapshot מטריקות

- **336** בדיקות עוברות (היה 306 בתחילת השבוע, +30 מהפתיחה).
- **7** commits ב-W18 (כולם 27.4).
- **3** ADRs סגורים; ללא חדשים השבוע.
- **2** פריטים פעילים ב-BACKLOG (BL-001, BL-002); 1 נסגר (BL-003).
- **0** סשנים אמיתיים — QA ידני עדיין לא קרה.
