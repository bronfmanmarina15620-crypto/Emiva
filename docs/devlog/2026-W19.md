# שבוע 2026-W19 (4–9 מאי)

*הערה: W18 (27 אפריל) לא קיבלה weeknote — הסוכן המתוזמן כנראה לא רץ,
ו-fallback ידני (§צ'ק-ליסט פתיחת שיחה) תפס רק עכשיו.
הטיוטה הזאת מכסה את W18 + W19 כפי שתועד ב-git log.*

---

## עלה

*W19 עצמה (4–8 מאי): אין commits.*

*W18 (27 אפריל) — כל העבודה הושלמה ביום אחד:*

- **ערבוב בעיות-כסף 3-מ-5** בתרגול של אוולין (MATH-EVELYN-MONEY-001) —
  60 פריטים חדשים בעברית (₪, הקשר יומיומי), שדה `context`+`explanation`
  אופציונלי על `AddSubItem`/`MultItem`, `selectNextItem` עם סינון הקשר,
  חיווט 3-מ-5 בדף הסשן. נאמן ל-MyLevel §3.1+§5.4.
- **מסלול הבנת הנקרא בעברית לאוולין** (CORE-HEBREW-EVELYN-002) —
  מיומנות `hebrew_comprehension` חדשה. 30 פריטים (פסקה קצרה + 2 שאלות
  אמריקאיות, 4 אפשרויות, 5 דרגות מעובדתי עד הסקה רב-שלבית). דף הסשן
  עוקב `currentQuestionIndex` ועובר Q1→Q2 על אותו טקסט.
  `parent-dashboard` עם מיפוי `SKILL_HEBREW` לעברית.
- **כמות פריטים בסשן נגזרת מגיל הפרופיל** (לא env var) —
  `itemsPerSessionForAge`: גיל 7–8 → 15, גיל 9–10 → 18. תוקן סחף שקט
  שבו `.env.local` קבע 15 לכולן בלי החלטה מתועדת.
- **גייט אופציונלי לאזור ההורים** — לחיצה על "הורים" בדף הבית פותחת
  קלט סיסמה כאשר `NEXT_PUBLIC_PARENT_GATE_CODE` מוגדר; ניווט ישיר כ-fallback.
  5 בדיקות UI חדשות.
- **הרשאות Claude Code משותפות** + תיקון `.gitignore` לאוברייד מקומי.

---

## הפתעות

- **BL-003 נפתח ונסגר באותו יום.** פתחנו backlog item לביצוע ערבוב כסף
  כ-variant עתידי — ואז קריאה צמודה של MyLevel §3.1+§5.4 גילתה שהערבוב
  הוא הוראת הליבה, לא "אולי מתישהו". נמחק ונכנס ישר לבנייה.
  לקח: לקרוא את המסמך לפני שפותחים backlog item.
- **CORE-HEBREW-EVELYN-001 (פוניקה) בוטל** — שתי הבנות כבר קוראות בשטף.
  פוניקה שיטתית הייתה מתחת לרמה. מיומנות ראשונה בתחום הקריאה קפצה
  ישר להבנת הנקרא.
- **בדיקה רופסת בגבול W17→W18** — selector מהתצוגה תפס גם
  `radio label` מחוץ לבלוק הנכון. תוקן ב-5f710cf (הדקת selector).
- **W18 בלי weeknote** — לא ברור אם הסוכן רץ ולא יצר draft, או שלא רץ.
  ראי §תקוע.

---

## תקוע

- **W18 weeknote לא תועדה.** המידע מ-W18 נכסה כאן בדיעבד, אבל
  `docs/devlog/2026-W18.md` לא קיים. יש להחליט: (א) ליצור W18 רטרואקטיבי
  (ערך היסטורי), (ב) להסתפק בכיסוי ב-W19. Marina מחליטה.
- **BL-002 (webpack cache):** טריגר אחד פעיל מ-2026-04-23 (שבירה בלי שינוי
  מבני). לא הגיע לסף ≥ 5 שבירות בשבוע עדיין. לא נחסם, אבל עוד לא
  נחקר.
- **אפס סשנים אמיתיים** — כל ה-QA עד כה ידני. הבנות לא ישבו מול
  האפליקציה עדיין.

---

## הבא

- **CORE-HEBREW-EMILIA-001** — הבנת הנקרא לאמיליה (טקסטים מאתגרים,
  הסקה מתקדמת, אוצר מילים). נמצא ב-🟡 הבא ב-ROADMAP.
- **סשן ראשון עם הבנות** — לפני שממשיכים לבנות, QA אנושי אמיתי.
  זה חשוב יותר מכל פיצ'ר הבא בתור.
- **(רשות) W18 retroactive** — אם Marina רוצה, Claude יכין טיוטה נפרדת.

---

## Snapshot מטריקות

| מטריקה | ערך |
|---|---|
| בדיקות עוברות | 336 (↑ מ-107 ב-W17; ↑ מ-322 לפני CORE-HEBREW-EVELYN-002) |
| מיומנויות פעילות — אוולין | `add_sub_100` · `multiplication` · `hebrew_comprehension` + ערבוב כסף 3/5 |
| מיומנויות פעילות — אמיליה | `fractions_intro` · `ops_1000` · `long_division` · `bar_models` |
| backlog פעיל | 2 (BL-001, BL-002) |
| backlog סגור | 1 (BL-003) |
| ADRs | 3 |
| סשנים אמיתיים | 0 (QA ידני בלבד) |
| commits השבוע (W19) | 0 |
| commits W18 | 7 (כולם 27 אפריל) |
