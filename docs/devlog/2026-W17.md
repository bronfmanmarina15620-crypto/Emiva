# Week of 2026-04-19 (W17)

*First weeknote. Today = entire week. Subsequent weeknotes cover Mon–Sun.*

## Shipped

- **Math MVP** from pre-scaffold to working session end-to-end: Mastery Gating + Adaptive Difficulty + Leitner SRS for Evelyn's add/sub-100. 30-item bank, 5 difficulty tiers. 35→61 unit tests as features grew.
- **Pedagogy layer:** 3-attempt retry before reveal. CPA-based Hebrew explanations (make-10, decomposition). Growth-mindset tone with banned-phrase list enforced.
- **Neo-Montessori UI:** cream/terracotta/sage palette, Heebo + Rubik via next/font, filling-jar mastery viz, canvas-confetti milestone celebration.
- **Welcome phase:** time + continuity-aware Hebrew greetings, name-prefixed.
- **4-layer backlog system:** BACKLOG.md + evals/backlog/ + telemetry.ts + FEEDBACK-LOG.md. CLAUDE.md §Backlog + §Pickup discipline codifies the rules.
- **Per-profile isolation (MATH-PROFILES-001):** profile picker, age-based content gating, storage scoped by profileId, names never in repo.
- **Brand (Emiva):** Logo + LogoMark + favicon. 3 ascending sage pills + wordmark with sage "i" as bridge letter. BRANDING.md codifies rules.
- **Folder rename:** `C:\Users\User\MyLevel` → `C:\Users\User\Emiva` (completed after VS Code restart).
- **Fractions introductory (MATH-BAT9-001 slice 1):** 26-item bank across 5 types, `FractionViz` SVG, per-skill routing, per-skill mastery storage with legacy migration. Emilia's track opens.
- **GitHub repo live:** `bronfmanmarina15620-crypto/Emiva` (public). Initial commit + 3 follow-ups.
- **ADR-002 + `/schedule` active:** two remote agents scheduled — weekly devlog (Fri 18:00 IDT) + monthly milestone (1st 09:00 IDT).
- **CLAUDE.md §Response format rewrite:** Micro tier added, IMPORTANT tokens, formatting rule, scaffold-when-applicable. Based on Anthropic Best Practices + agent research.
- **Fix — stale allowedSkills on existing profiles:** `loadProfiles` re-derives from age on read. Curriculum updates now propagate automatically.

## Surprises

- **Naming rabbit hole.** Spent 3 iterations (MyLevel → Sulam → Loomi → Evami → Emiva). Each name needed web research to clear: Sulam had the Israeli special-ed org blocking; Loomi had TWO direct ed-tech competitors. **Lesson:** 7-point check *before* recommending. Saved as feedback memory for future-Claude.
- **Webpack/HMR cache brittleness.** Dev server broke 3 times after large structural edits (fonts added, tailwind config changed, storage keys renamed). Fix each time: stop dev → clear `.next/` → restart. If this continues, worth a BACKLOG entry with trigger "≥ 5 breaks in a week".
- **Sanskrit bonus on Emiva** — "one who has the knowledge of truth" — pure serendipity. Research discovered it *after* Marina chose the name.
- **Recurring pattern: "pattern-match on headers, skip reading content."** Identified mid-session and turned into two feedback memories (`feedback_quote_before_proposing.md`, `feedback_implement_not_reopen.md`). Wasted ~20 min on a proposal to duplicate MyLevel.docx into `docs/curriculum/`, plus ~15 min presenting 4-axis design menus for questions already answered in the spec. Honest cost: Marina had to catch both in real time.
- **`.claude/rules/` path-scoping only saves context if `paths:` YAML frontmatter is present.** I created `pedagogy.md` with path scope in prose instead of frontmatter — zero context savings. Also moved cross-cutting rules (Measurement, Research) out of CLAUDE.md before realising they fire on decisions (not file reads) and must stay in base context. Reverted.

## Stuck

*(ריק)*

## Next

- **Manual QA of Emilia fractions session** — not yet completed. Stale-profile bug interrupted the first attempt; fix verified via unit test, not via full browser session.
- **MATH-BAT9-002** — ops up to 1000 (Emilia slice 2). Waiting on real usage data from slice 1.
- **MATH-BAT9-003** — long division (Emilia slice 3). After slice 2.
- **MATH-EMILIA-BARMODELS-001** — Bar Models for word problems. Reuses `FractionViz` visual language.
- If real user activity starts, raise `evals/backlog/feedback-variety.eval.ts` REQUIRED_MIN from 3 → 5 and start expanding variant pools.

## Metric snapshot

- Files in repo: ~45 (source) + 8 docs + 11 tests + 6 tasks + 2 configs.
- Tests: **107 passing** (was 61 at end of Day 1 content work).
- Evals (backlog): 5 passing.
- Active profiles in localStorage: 0 (manual QA scheduled after Evelyn / Emilia arrival).
- Decisions turned into ADRs: 2 (Layered Backlog System, Scheduled Rituals).
- Remote triggers active: 2 (weekly devlog, monthly milestone).
- GitHub repo: `bronfmanmarina15620-crypto/Emiva` (public), 4 commits on `main`.
- CLAUDE.md: 266 lines (target 200, 33% over). Content is cross-cutting and justified.
